{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://transcriptforge.dev/schemas/nextflow-params/1.0.0",
  "title": "TranscriptForge workflow parameters",
  "type": "object",
  "properties": {
    "outdir": {
      "type": "string",
      "description": "Immutable publication directory for workflow outputs"
    },
    "dataset_manifest": {
      "type": "string",
      "description": "Path to a validated Dataset Manifest"
    },
    "validation_config": {
      "type": "string",
      "description": "Path to matrix validation configuration JSON"
    },
    "matrix": {
      "type": "string",
      "description": "Path to a count or expression matrix"
    },
    "metadata": {
      "type": "string",
      "description": "Path to sample metadata"
    },
    "expression_bundle": {
      "type": "string",
      "description": "Path to a validated Expression Bundle"
    },
    "analysis_request": {
      "type": "string",
      "description": "Path to a frozen Analysis Request"
    }
  },
  "additionalProperties": true
}
