{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://transcriptforge.dev/schemas/nextflow-params/1.0.0",
  "title": "TranscriptForge workflow parameters",
  "type": "object",
  "properties": {
    "outdir": {
      "type": "string",
      "description": "Immutable publication directory for workflow outputs"
    },
    "dataset_manifest": {
      "type": "string",
      "description": "Path to a validated Dataset Manifest"
    },
    "validation_config": {
      "type": "string",
      "description": "Path to matrix validation configuration JSON"
    },
    "matrix": {
      "type": "string",
      "description": "Path to a count or expression matrix"
    },
    "metadata": {
      "type": "string",
      "description": "Path to sample metadata"
    },
    "expression_bundle": {
      "type": "string",
      "description": "Path to a validated Expression Bundle"
    },
    "analysis_request": {
      "type": "string",
      "description": "Path to a frozen Analysis Request"
    },
    "ingestion_manifest": {
      "type": "string",
      "description": "Path to a frozen raw RNA-seq or Affymetrix ingestion manifest"
    },
    "cels": {
      "type": "string",
      "description": "Directory containing checksum-frozen Affymetrix CEL files"
    },
    "aws_region": {
      "type": "string",
      "description": "AWS Region containing the Batch queue and S3 work bucket"
    },
    "aws_batch_queue": {
      "type": "string",
      "description": "AWS Batch job queue name or ARN"
    },
    "aws_batch_job_role_arn": {
      "type": "string",
      "description": "Least-privilege IAM role assumed by Batch tasks"
    },
    "aws_batch_log_group": {
      "type": "string",
      "description": "CloudWatch Logs group for Batch task logs"
    },
    "aws_scientific_image": {
      "type": "string",
      "description": "ECR scientific image pinned by sha256 digest"
    },
    "aws_reference_cache_uri": {
      "type": "string",
      "description": "S3 prefix for immutable checksum-keyed reference materializations"
    }
  },
  "additionalProperties": true
}
